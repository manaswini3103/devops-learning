# Cookies

- These are rarely used in APIs
- cookies are sent as headers 

![API](../images/cookies.jpg) 

- The response may contain a header called set-cookie
- the request may contain a header called cookie.
- cookies are way for server to save small amounts of data on client computer, so when client comes back to server, it recognize that it is the same client or has some additional information about it.
